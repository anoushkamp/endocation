<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Latest Research | Endocation</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
  </head>

  <body>
    <header class="site-header">
      <a href="/" class="brand">
        <img
          src="/endocation-logo.png"
          alt="Endocation logo"
          class="brand-logo"
        />
        <span>ENDOCATION</span>
      </a>

      <nav>
  <a href="/research" class="active" data-astro-reload>Latest Research</a>
  <a href="/endometriosis-101" data-astro-reload>Endometriosis 101</a>
  <a href="/advocate" data-astro-reload>Advocate</a>
  <a href="/about" data-astro-reload>About</a>
  <a href="/contact" data-astro-reload>Contact</a>
</nav>
    </header>

    <main>
      <section class="research-hero">
        <p class="eyebrow">LATEST RESEARCH</p>

        <h1>
          Women's health research.
          <span>Translated.</span>
        </h1>

        <p class="intro">
          New papers in endometriosis and women's health, broken down into what
          researchers found, what the evidence actually means, and what it
          doesn't.
        </p>
      </section>

      <section class="research-feed">
        <div class="feed-heading">
          <p class="eyebrow">THE BREAKDOWNS</p>
          <h2>Read the research without the jargon.</h2>
        </div>

       <div class="first-article">
  <div class="article-number">01</div>

  <div class="article-content">
    <p class="article-label">NEW · 5 MIN READ</p>

    <h3>Excision versus Ablation: Which is better for endometriosis-related pain?</h3>

    <p>
      A breakdown of Ouyang et al.'s (2022) systematic review and
      meta-analysis comparing excision and ablation for
      endometriosis-related pain.
    </p>

    <a href="/research/first-paper" class="article-link">
      Read the breakdown →
    </a>
  </div>
</div>
      </section>

      <section class="method">
        <p class="eyebrow yellow">HOW ENDOCATION READS A PAPER</p>

        <div class="method-grid">
          <h2>Interesting doesn't automatically mean clinically useful.</h2>

          <div>
            <p>
              Each article looks beyond the headline: study design, sample
              size, limitations, context and whether the conclusions actually
              match the evidence.
            </p>

            <p>
              Endocation articles are educational and do not replace
              personalised medical advice.
            </p>
          </div>
        </div>
      </section>
    </main>

    <footer>
      <a href="/" class="brand footer-brand">
        <img
          src="/endocation-logo.png"
          alt="Endocation logo"
          class="brand-logo"
        />
        <span>ENDOCATION</span>
      </a>

      <p>
        Evidence-led women's health education. Not individual medical advice.
      </p>

      <p>© 2026 ENDOCATION</p>
    </footer>
  </body>
</html>

<style>
  :global(*) {
    box-sizing: border-box;
  }

  :global(body) {
    margin: 0;
    background: #fffaf2;
    color: #261235;
    font-family: "Manrope", sans-serif;
  }

  :global(a) {
    color: inherit;
  }

  .site-header {
    width: min(1400px, calc(100% - 64px));
    margin: 0 auto;
    min-height: 90px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 40px;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 12px;
    text-decoration: none;
    font-size: 1rem;
    font-weight: 800;
    letter-spacing: -0.05em;
  }

  .brand-logo {
    width: 48px;
    height: 48px;
    object-fit: contain;
    border-radius: 50%;
  }

  nav {
    display: flex;
    gap: 30px;
  }

  nav a {
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 600;
  }
nav a.active {
  color: #7a2cff;
}
  nav a:hover {
    color: #7a2cff;
  }

  .research-hero {
    width: min(1400px, calc(100% - 64px));
    margin: 0 auto;
    padding: 110px 0 120px;
  }

  .eyebrow {
    margin: 0 0 20px;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.14em;
  }

  .research-hero h1 {
    max-width: 1100px;
    margin: 0;
    font-size: clamp(4rem, 8vw, 8rem);
    line-height: 0.92;
    letter-spacing: -0.075em;
  }

  .research-hero h1 span {
    display: block;
    color: #7a2cff;
  }

  .intro {
    max-width: 720px;
    margin: 38px 0 0;
    font-size: 1.15rem;
    line-height: 1.8;
  }

  .research-feed {
    padding: 100px max(32px, calc((100% - 1400px) / 2));
    background: #f0eaff;
  }

  .feed-heading {
    max-width: 850px;
    margin-bottom: 60px;
  }

  .feed-heading h2 {
    margin: 0;
    font-size: clamp(2.8rem, 5vw, 5rem);
    line-height: 1;
    letter-spacing: -0.06em;
  }

  .first-article {
    min-height: 370px;
    background: #fffaf2;
    border-radius: 28px;
    padding: 36px;
    display: grid;
    grid-template-columns: 100px 1fr;
    align-items: start;
  }

  .article-number {
    font-size: 0.75rem;
    font-weight: 800;
    color: #7a2cff;
  }

  .article-content {
    max-width: 900px;
  }

  .article-label {
    margin: 0 0 30px;
    font-size: 0.7rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    color: #7a2cff;
  }

  .article-content h3 {
    margin: 0;
    max-width: 850px;
    font-size: clamp(2.3rem, 4vw, 4.3rem);
    line-height: 1.05;
    letter-spacing: -0.055em;
  }

  .article-content > p {
    margin-top: 30px;
    max-width: 650px;
    line-height: 1.8;
  }

  .article-link {
    display: inline-block;
    margin-top: 28px;
    font-weight: 800;
    text-underline-offset: 5px;
  }

  .method {
    padding: 110px max(32px, calc((100% - 1400px) / 2));
    background: #6d24df;
    color: white;
  }

  .yellow {
    color: #f1ff38;
  }

  .method-grid {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 100px;
  }

  .method h2 {
    margin: 0;
    font-size: clamp(2.8rem, 5vw, 5rem);
    line-height: 1;
    letter-spacing: -0.06em;
  }

  .method-grid p {
    margin-top: 0;
    line-height: 1.8;
  }

  footer {
    padding: 55px max(32px, calc((100% - 1400px) / 2));
    background: #261235;
    color: white;
    display: grid;
    grid-template-columns: 1fr 1fr auto;
    gap: 40px;
    align-items: center;
  }

  footer p {
    margin: 0;
    font-size: 0.8rem;
  }

  .footer-brand {
    color: #f1ff38;
  }

  @media (max-width: 900px) {
   nav {
  display: flex;
  flex-wrap: wrap;
  gap: 12px 20px;
  width: 100%;
  margin-top: 8px;
}

nav a {
  font-size: 0.95rem;
}

   .site-header {
  width: calc(100% - 40px);
  flex-direction: column;
  align-items: flex-start;
  gap: 20px;
  padding-top: 20px;
  padding-bottom: 20px;
}

    .research-hero {
      width: calc(100% - 40px);
      padding: 70px 0 90px;
    }

    .research-feed,
    .method {
      padding-left: 20px;
      padding-right: 20px;
    }

    .first-article {
      grid-template-columns: 1fr;
      gap: 25px;
    }

    .method-grid {
      grid-template-columns: 1fr;
      gap: 45px;
    }

    footer {
      grid-template-columns: 1fr;
    }
  }
</style>